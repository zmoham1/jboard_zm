__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
config.yaml
src/profile_local.py
# Every local resume override, not just the two that existed first.
# evaluation.py also reads candidate_evidence.local.md, which the explicit
# list missed — a glob keeps future .local files out of the repo by default.
data/resume/*.local.*

# Local workstation state
state/*.db
state/*.db-shm
state/*.db-wal
state/*.csv
!state/gha-jobs.db
!state/gha-dead_boards.csv
